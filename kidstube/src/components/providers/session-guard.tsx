"use client";

/**
 * Placeholder: la sesión con RefreshAccessTokenError se trata como invitado
 * (useYouTubeAuth / resolveYoutubeAccess), sin signOut en bucle.
 */
export function SessionGuard({ children }: { children: React.ReactNode }) {
  return children;
}
